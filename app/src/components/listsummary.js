const ListSummary = (item) => {
  //const { Text, Description } = item;
  console.log(item);
  return (
    <div>
      <h2>Meals</h2>
      <p>Summary of meals.</p>
    </div>
  );
};

export default ListSummary;
